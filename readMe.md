# 简单 Node.js 示例

这是一个最小的 Node.js 示例项目，包含示例运行和服务器示例。

运行应用：

```bash
# 直接运行
node app.js

# 或使用 npm 脚本
npm start
```

服务器示例：

```bash
# 启动示例 HTTP 服务器
node server.js

# 或使用 npm 脚本
npm run server
```

可访问：
- 根路径: http://localhost:3000/ 返回文本
- JSON: http://localhost:3000/json 返回 JSON

